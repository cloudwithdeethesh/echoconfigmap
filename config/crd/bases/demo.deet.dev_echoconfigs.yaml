---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: echoconfigs.demo.deet.dev
spec:
  group: demo.deet.dev
  names:
    kind: EchoConfig
    listKind: EchoConfigList
    plural: echoconfigs
    shortNames:
    - echo
    singular: echoconfig
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.message
      name: Message
      type: string
    - jsonPath: .status.configMapName
      name: ConfigMap
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              message:
                description: Message is mirrored into the ConfigMap's data.message
                minLength: 1
                type: string
            required:
            - message
            type: object
          status:
            properties:
              configMapName:
                description: Name of the managed ConfigMap
                type: string
              observedGeneration:
                description: For knowing if status matches current spec generation
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
